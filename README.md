# 日本株 自動取得セットアップガイド

毎日18時に自動実行 → Google スプレッドシートに書き込む仕組みです。
初回セットアップは約1〜2時間で完了します。

---

## ファイル構成

```
stock_updater/
├── fetch_stocks.py              # メインスクリプト
├── requirements.txt             # 必要ライブラリ
├── README.md                    # このファイル
└── .github/
    └── workflows/
        └── fetch_stocks.yml     # GitHub Actions 設定
```

---

## セットアップ手順

### ステップ1: Google スプレッドシートを作成

1. Google スプレッドシートを新規作成
2. URLから **スプレッドシートID** をコピーしておく
   - 例: `https://docs.google.com/spreadsheets/d/【ここがID】/edit`

---

### ステップ2: Google Cloud でサービスアカウントを作成

1. https://console.cloud.google.com/ にアクセス
2. 新しいプロジェクトを作成（例: `stock-updater`）
3. 左メニュー「APIとサービス」→「ライブラリ」
   - 「Google Sheets API」を有効化
   - 「Google Drive API」を有効化
4. 「APIとサービス」→「認証情報」→「認証情報を作成」→「サービスアカウント」
5. 名前を入力（例: `stock-bot`）して作成
6. 作成したサービスアカウントをクリック →「キー」タブ →「鍵を追加」→「JSON」
7. ダウンロードされた JSON ファイルの**中身全体**をコピーしておく

---

### ステップ3: スプレッドシートをサービスアカウントと共有

1. ダウンロードした JSON を開き `"client_email"` の値をコピー
   - 例: `stock-bot@stock-updater.iam.gserviceaccount.com`
2. Google スプレッドシートを開き「共有」ボタン
3. 上記のメールアドレスを「編集者」として追加

---

### ステップ4: GitHubリポジトリを作成してファイルをアップロード

1. GitHub で新しいリポジトリを作成（プライベートでOK）
2. このフォルダ内のファイルをすべてアップロード
   ```
   fetch_stocks.py
   requirements.txt
   .github/workflows/fetch_stocks.yml
   ```

---

### ステップ5: GitHub Secrets を設定

リポジトリの「Settings」→「Secrets and variables」→「Actions」→「New repository secret」

| Secret名 | 値 |
|----------|----|
| `SPREADSHEET_ID` | ステップ1でコピーしたID |
| `GOOGLE_CREDENTIALS_JSON` | ステップ2でコピーしたJSONの中身全体 |

---

### ステップ6: 銘柄コードを追加

`fetch_stocks.py` の `TICKERS` リストに300社分の銘柄コードを追加します。

```python
TICKERS = [
    "7203.T",  # トヨタ自動車
    "6758.T",  # ソニーグループ
    # ... 残りを追加
]
```

**銘柄コードの調べ方**: 証券コード（4桁）の後ろに `.T` をつけるだけです。
- 例: トヨタ(7203) → `7203.T`

---

### ステップ7: 動作確認（手動実行）

1. GitHubリポジトリの「Actions」タブを開く
2. 「毎日株価取得」ワークフローを選択
3. 「Run workflow」ボタンで手動実行
4. 緑のチェックマークが出ればOK！
5. スプレッドシートを確認してデータが入っていれば完成

---

## 自動実行スケジュール

- **実行時刻**: 毎日 18:00 JST（平日のみ・月〜金）
- 東証の取引終了（15:30）後に十分な余裕を持って実行されます
- 祝日は自動判定していないため、祝日もスクリプトは走りますが、
  yfinance側でデータがない日は取得スキップされます

---

## スプレッドシートの構成

### 「最新株価」シート
毎日上書き更新。常に最新のデータが入ります。

| 列 | 内容 |
|----|------|
| A | 銘柄コード |
| B | 銘柄名 |
| C | 取得日 |
| D | 始値 |
| E | 高値 |
| F | 安値 |
| G | 終値 |
| H | 出来高 |
| I | 前日終値 |
| J | 前日比(円) |
| K | 前日比(%) |

### 「履歴」シート
毎日追記。日々のデータが蓄積されていきます。

---

## トラブルシューティング

**Actionsが失敗した場合**
- GitHubの「Actions」タブでエラーログを確認
- Secretsの設定が正しいか再確認
- サービスアカウントのメールがスプレッドシートと共有されているか確認

**データが一部取得できない銘柄がある場合**
- 銘柄コードが正しいか確認（末尾に `.T` があるか）
- その日が休場日の可能性あり（正常動作です）
